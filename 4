
abstract class Shape
{
    public abstract int GetArea();
}

class Square : Shape
{
    int side;

    public Square(int n) => side = n;

    // GetArea method is required to avoid a compile-time error.
    public override int GetArea() => side * side;

    static void Main()
    {
        var sq = new Square(12);
        Console.writeline($"Area of the square = {sq.GetArea()}");
        
    }
}
//The following code is an example of the abstract modifier
//Also exemplifies how things being modified have missing or incomplete implementations
//Also shows that members marked as abstract must be implemented by classes that derive from an abstract class

//taken from Microsoft and can be found with the following link
//https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/keywords/abstract

// Output: Area of the square = 144



